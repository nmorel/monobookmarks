{
  "name": "@nimo/slugify",
  "version": "1.0.0",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "@nimo/config-babel": "*",
    "@nimo/config-eslint": "*",
    "@nimo/config-test": "*",
    "@nimo/config-ts": "*"
  },
  "scripts": {
    "build": "rm -rf dist && yarn build:esm && yarn build:cjs",
    "build:esm": "tsc -p tsconfig.json",
    "build:cjs": "tsc -p tsconfig-cjs.json",
    "lint": "eslint .",
    "start": "FAST_REFRESH=false webpack serve",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "tscheck": "tsc --noEmit"
  }
}
